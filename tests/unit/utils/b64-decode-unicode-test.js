import b64DecodeUnicode from 'dummy/utils/b64-decode-unicode';
import { module, test } from 'qunit';

module('Unit | Utility | b64 decode unicode');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = b64DecodeUnicode();
  assert.ok(result);
});
