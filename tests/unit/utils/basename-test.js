import basename from 'dummy/utils/basename';
import { module, test } from 'qunit';

module('Unit | Utility | basename');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = basename();
  assert.ok(result);
});
