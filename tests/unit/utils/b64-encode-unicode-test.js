import b64EncodeUnicode from 'dummy/utils/b64-encode-unicode';
import { module, test } from 'qunit';

module('Unit | Utility | b64 encode unicode');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = b64EncodeUnicode();
  assert.ok(result);
});
