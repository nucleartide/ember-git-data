import arrayRemove from 'dummy/utils/array-remove';
import { module, test } from 'qunit';

module('Unit | Utility | array remove');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayRemove();
  assert.ok(result);
});
